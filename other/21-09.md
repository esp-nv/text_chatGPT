````text
1.	може ли да напишеш стъпките за създаване на ASP.NET Core MVC
2.	ChatGPT said:
Разбира се. Ето основните стъпки за създаване на ASP.NET Core MVC приложение във Visual Studio:
1. Инсталиране на необходимите инструменти
Инсталирай:
o	Visual Studio 2022/2026
o	Workload ASP.NET and web development
o	Подходящ .NET SDK (например .NET 8/9/10 според изискванията на проекта)
2. Създаване на нов проект
В Visual Studio:
4.	Избери Create a new project.
5.	Потърси ASP.NET Core Web App (Model-View-Controller).
6.	Избери шаблона и натисни Next.
7.	Въведи:
	Project name – например MyMvcApp
	Location – папката, в която ще бъде проектът.
8.	Натисни Next.
3. Конфигуриране на проекта
В следващия екран избери:
o	Framework – например .NET 8.0 или друга необходима версия.
o	Authentication type – None, ако няма да използваш потребителски акаунти.
o	Configure for HTTPS – включено.
o	Enable Docker – изключено, ако Docker не е необходим.
След това натисни Create.
4. Разглеждане на структурата
Създаденият проект обикновено има структура:
MyMvcApp
│
├── Controllers
│   └── HomeController.cs
│
├── Models
│   └── ErrorViewModel.cs
│
├── Views
│   ├── Home
│   │   ├── Index.cshtml
│   │   └── Privacy.cshtml
│   └── Shared
│       └── _Layout.cshtml
│
├── wwwroot
│   ├── css
│   ├── js
│   └── lib
│
├── Program.cs
└── appsettings.json
MVC означава:
o	Model – данните и бизнес логиката.
o	View – HTML страниците (.cshtml).
o	Controller – обработва HTTP заявките и избира подходящия View.
5. Създаване на Controller
Например:
using Microsoft.AspNetCore.Mvc;

namespace MyMvcApp.Controllers
{
    public class ProductsController : Controller
    {
        public IActionResult Index()
        {
            return View();
        }
    }
}
6. Създаване на View
Създай:
Views
└── Products
    └── Index.cshtml
Например:
@{
    ViewData["Title"] = "Products";
}

<h1>Products</h1>

<p>Това е страницата с продуктите.</p>
Когато отвориш:
/Products
MVC ще извика ProductsController → Index() → Views/Products/Index.cshtml.
7. Създаване на Model
Например:
namespace MyMvcApp.Models
{
    public class Product
    {
        public int Id { get; set; }
        public string Name { get; set; }
        public decimal Price { get; set; }
    }
}
След това Model-ът може да бъде подаден от Controller към View.
8. Стартиране на приложението
Натисни:
Ctrl + F5
или бутона ▶ във Visual Studio.
Ще се стартира локален адрес от типа:
https://localhost:xxxx
9. Основният MVC поток
Когато потребителят отвори:
/Products/Details/5
обикновено се случва следното:
Browser
   ↓
Routing
   ↓
ProductsController
   ↓
Details(5)
   ↓
Model / Database
   ↓
View
   ↓
HTML
   ↓
Browser
Ако искаш да го правиш като учебна задача, мога да ти покажа и цял пример стъпка по стъпка: ASP.NET Core MVC + Entity Framework Core + SQL Server + CRUD (Create, Read, Update, Delete).
мога да ти покажа и цял пример стъпка по стъпка
3.	You said:
като малък проект
4.	ChatGPT said:
Да. Ако е малък учебен проект, можеш да го направиш много по-просто. Например проект „Списък с книги“, в който можеш да добавяш, разглеждаш, редактираш и изтриваш книги.
Малък ASP.NET Core MVC проект – „Books“
1. Създаване на проекта
В Visual Studio:
0.	Create a new project
1.	Избираш ASP.NET Core Web App (Model-View-Controller)
2.	Име: BookStore
3.	Избираш подходящата .NET версия
4.	Create
2. Създаване на Model
В Models създаваш Book.cs:
namespace BookStore.Models
{
    public class Book
    {
        public int Id { get; set; }

        public string Title { get; set; }

        public string Author { get; set; }

        public decimal Price { get; set; }
    }
}
Това описва една книга.
3. Създаване на Controller
В Controllers създаваш BooksController.cs:
using Microsoft.AspNetCore.Mvc;
using BookStore.Models;

namespace BookStore.Controllers
{
    public class BooksController : Controller
    {
        public IActionResult Index()
        {
            var books = new List<Book>
            {
                new Book { Id = 1, Title = "Под игото", Author = "Иван Вазов", Price = 15 },
                new Book { Id = 2, Title = "Бай Ганьо", Author = "Алеко Константинов", Price = 12 },
                new Book { Id = 3, Title = "Тютюн", Author = "Димитър Димов", Price = 18 }
            };

            return View(books);
        }
    }
}
Тук засега използваме списък в паметта, вместо база данни. За малък проект това е достатъчно.
4. Създаване на View
Създаваш папка:
Views
└── Books
    └── Index.cshtml
В Index.cshtml:
@model List<BookStore.Models.Book>

<h1>Книги</h1>

<table class="table">
    <thead>
        <tr>
            <th>Заглавие</th>
            <th>Автор</th>
            <th>Цена</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var book in Model)
        {
            <tr>
                <td>@book.Title</td>
                <td>@book.Author</td>
                <td>@book.Price лв.</td>
            </tr>
        }
    </tbody>
</table>
5. Стартиране
Стартираш проекта с Ctrl + F5.
След това отваряш:
/Books
Например:
https://localhost:7000/Books
Ще получиш таблица:
Заглавие	Автор	Цена
Под игото	Иван Вазов	15 лв.
Бай Ганьо	Алеко Константинов	12 лв.
Тютюн	Димитър Димов	18 лв.
Крайна структура
BookStore
│
├── Controllers
│   ├── HomeController.cs
│   └── BooksController.cs
│
├── Models
│   ├── ErrorViewModel.cs
│   └── Book.cs
│
├── Views
│   ├── Home
│   ├── Books
│   │   └── Index.cshtml
│   └── Shared
│
├── wwwroot
│
├── Program.cs
└── appsettings.json
Това вече е малък, напълно реален MVC проект, в който можеш да покажеш трите основни части: Model → Controller → View.
Ако проектът е за училище/университет, следващата логична стъпка е да добавим Create, Edit и Delete, но пак без база данни, за да остане малък и лесен.
5.	You said:
малък, напълно реален MVC проект
1.	Да — ако целта е малък, но напълно реален ASP.NET Core MVC проект, бих ти предложил „BookStore“ – приложение за управление на книги.
То ще има:
o	Model – Book
o	Controller – BooksController
o	Views – списък, добавяне, редактиране и изтриване
o	Entity Framework Core
o	SQLite база данни
o	CRUD операции
o	Bootstrap за по-добър интерфейс
Структура
BookStore
│
├── Controllers
│   └── BooksController.cs
│
├── Data
│   └── ApplicationDbContext.cs
│
├── Models
│   └── Book.cs
│
├── Views
│   ├── Books
│   │   ├── Index.cshtml
│   │   ├── Create.cshtml
│   │   ├── Edit.cshtml
│   │   ├── Details.cshtml
│   │   └── Delete.cshtml
│   │
│   └── Shared
│       └── _Layout.cshtml
│
├── wwwroot
│
├── appsettings.json
└── Program.cs
Как ще работи
Потребителят отваря:
/Books
Получава списък:
-----------------------------------------
 Книги
-----------------------------------------
 Заглавие       Автор          Цена
 Под игото      Иван Вазов     15.00 лв.
 Тютюн          Д. Димов       18.00 лв.

 [Добави книга]
-----------------------------------------
При Добави книга се отваря форма:
Заглавие: [________________]

Автор:    [________________]

Цена:     [________________]

          [Запази]
Данните се записват в SQLite.
След това ще можеш да:
Книги
------------------------------------------------
Заглавие     Автор          Цена    Действия
------------------------------------------------
Под игото    Иван Вазов     15      Детайли
                                      Редактирай
                                      Изтрий
Така проектът вече демонстрира реалния MVC модел:
             ┌──────────────┐
             │    Browser   │
             └──────┬───────┘
                    ↓
             ┌──────────────┐
             │  Controller  │
             └──────┬───────┘
                    ↓
             ┌──────────────┐
             │     Model    │
             └──────┬───────┘
                    ↓
             ┌──────────────┐
             │   Database   │
             └──────┬───────┘
                    ↓
             ┌──────────────┐
             │     View     │
             └──────────────┘
Това е добър размер за учебен проект — не е прекалено сложен, но показва основните неща, които обикновено се очакват от един ASP.NET Core MVC проект.
Ако искаш, мога да ти дам целия проект от нулата, стъпка по стъпка, с целия код за всеки файл, така че само да го създадеш във Visual Studio и да го стартираш.
2.	You said:
да
3.	ChatGPT said:
Разбира се. Нека направим малък, но напълно реален ASP.NET Core MVC проект „BookStore“ с SQLite база данни и CRUD операции.
Ще използваме ASP.NET Core MVC + Entity Framework Core + SQLite.
1. Създай проекта
В Visual Studio:
1.	Create a new project
2.	Избери ASP.NET Core Web App (Model-View-Controller)
3.	Име на проекта: BookStore
4.	Избери актуалната налична .NET версия
5.	Authentication: None
6.	Натисни Create
________________________________________
2. Инсталирай Entity Framework Core
Отвори:
Tools → NuGet Package Manager → Package Manager Console
Изпълни:
Install-Package Microsoft.EntityFrameworkCore.Sqlite
Install-Package Microsoft.EntityFrameworkCore.Tools
Това ще ни позволи да използваме SQLite и Entity Framework Core.
________________________________________
3. Създай Model
В папката Models създай:
Book.cs
Book.cs
using System.ComponentModel.DataAnnotations;

namespace BookStore.Models
{
    public class Book
    {
        public int Id { get; set; }

        [Required(ErrorMessage = "Въведете заглавие.")]
        public string Title { get; set; } = string.Empty;

        [Required(ErrorMessage = "Въведете автор.")]
        public string Author { get; set; } = string.Empty;

        [Range(0.01, 10000, ErrorMessage = "Цената трябва да бъде между 0.01 и 10000.")]
        public decimal Price { get; set; }
    }
}
Това е нашият Model.
Той описва какво представлява една книга:
o	Id
o	Title
o	Author
o	Price
________________________________________
4. Създай Database Context
Създай нова папка:
Data
В нея създай:
ApplicationDbContext.cs
ApplicationDbContext.cs
using BookStore.Models;
using Microsoft.EntityFrameworkCore;

namespace BookStore.Data
{
    public class ApplicationDbContext : DbContext
    {
        public ApplicationDbContext(DbContextOptions<ApplicationDbContext> options)
            : base(options)
        {
        }

        public DbSet<Book> Books { get; set; }
    }
}
DbSet<Book> казва на Entity Framework, че ще имаме таблица за книгите.
________________________________________
5. Настрой SQLite
Отвори appsettings.json.
Добави connection string:
appsettings.json
{
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=bookstore.db"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*"
}
Базата данни ще бъде файл:
bookstore.db
в проекта.
________________________________________
6. Промени Program.cs
Отвори Program.cs.
Замени съдържанието му с:
Program.cs
using BookStore.Data;
using Microsoft.EntityFrameworkCore;

var builder = WebApplication.CreateBuilder(args);

// MVC
builder.Services.AddControllersWithViews();

// Database
builder.Services.AddDbContext<ApplicationDbContext>(options =>
    options.UseSqlite(
        builder.Configuration.GetConnectionString("DefaultConnection")));

var app = builder.Build();

if (!app.Environment.IsDevelopment())
{
    app.UseExceptionHandler("/Home/Error");
    app.UseHsts();
}

app.UseHttpsRedirection();

app.UseStaticFiles();

app.UseRouting();

app.UseAuthorization();

app.MapControllerRoute(
    name: "default",
    pattern: "{controller=Home}/{action=Index}/{id?}");

app.Run();
Тук свързваме MVC приложението със SQLite базата.
________________________________________
7. Създай базата данни
Отвори:
Tools → NuGet Package Manager → Package Manager Console
Изпълни:
Add-Migration InitialCreate
След това:
Update-Database
Entity Framework ще създаде:
bookstore.db
и таблица:
Books
________________________________________
8. Създай BooksController
В папката:
Controllers
създай:
BooksController.cs
BooksController.cs
using BookStore.Data;
using BookStore.Models;
using Microsoft.AspNetCore.Mvc;
using Microsoft.EntityFrameworkCore;

namespace BookStore.Controllers
{
    public class BooksController : Controller
    {
        private readonly ApplicationDbContext _context;

        public BooksController(ApplicationDbContext context)
        {
            _context = context;
        }

        // GET: /Books
        public async Task<IActionResult> Index()
        {
            var books = await _context.Books.ToListAsync();

            return View(books);
        }

        // GET: /Books/Details/5
        public async Task<IActionResult> Details(int? id)
        {
            if (id == null)
                return NotFound();

            var book = await _context.Books
                .FirstOrDefaultAsync(b => b.Id == id);

            if (book == null)
                return NotFound();

            return View(book);
        }

        // GET: /Books/Create
        public IActionResult Create()
        {
            return View();
        }

        // POST: /Books/Create
        [HttpPost]
        [ValidateAntiForgeryToken]
        public async Task<IActionResult> Create(Book book)
        {
            if (ModelState.IsValid)
            {
                _context.Books.Add(book);
                await _context.SaveChangesAsync();

                return RedirectToAction(nameof(Index));
            }

            return View(book);
        }

        // GET: /Books/Edit/5
        public async Task<IActionResult> Edit(int? id)
        {
            if (id == null)
                return NotFound();

            var book = await _context.Books.FindAsync(id);

            if (book == null)
                return NotFound();

            return View(book);
        }

        // POST: /Books/Edit/5
        [HttpPost]
        [ValidateAntiForgeryToken]
        public async Task<IActionResult> Edit(int id, Book book)
        {
            if (id != book.Id)
                return NotFound();

            if (ModelState.IsValid)
            {
                _context.Update(book);
                await _context.SaveChangesAsync();

                return RedirectToAction(nameof(Index));
            }

            return View(book);
        }

        // GET: /Books/Delete/5
        public async Task<IActionResult> Delete(int? id)
        {
            if (id == null)
                return NotFound();

            var book = await _context.Books
                .FirstOrDefaultAsync(b => b.Id == id);

            if (book == null)
                return NotFound();

            return View(book);
        }

        // POST: /Books/Delete/5
        [HttpPost, ActionName("Delete")]
        [ValidateAntiForgeryToken]
        public async Task<IActionResult> DeleteConfirmed(int id)
        {
            var book = await _context.Books.FindAsync(id);

            if (book != null)
            {
                _context.Books.Remove(book);
                await _context.SaveChangesAsync();
            }

            return RedirectToAction(nameof(Index));
        }
    }
}
Този Controller вече реализира целия CRUD:
Create → добавяне
Read   → преглеждане
Update → редактиране
Delete → изтриване
________________________________________
9. Създай Views
В Views създай папка:
Books
Тя трябва да изглежда така:
Views
└── Books
    ├── Index.cshtml
    ├── Create.cshtml
    ├── Edit.cshtml
    ├── Details.cshtml
    └── Delete.cshtml
Index.cshtml
Views/Books/Index.cshtml
@model IEnumerable<BookStore.Models.Book>

@{
    ViewData["Title"] = "Книги";
}

<h1>Книги</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">
        Добави книга
    </a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Заглавие</th>
            <th>Автор</th>
            <th>Цена</th>
            <th>Действия</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var book in Model)
        {
            <tr>
                <td>@book.Title</td>
                <td>@book.Author</td>
                <td>@book.Price.ToString("0.00") лв.</td>

                <td>
                    <a asp-action="Details"
                       asp-route-id="@book.Id"
                       class="btn btn-info btn-sm">
                        Детайли
                    </a>

                    <a asp-action="Edit"
                       asp-route-id="@book.Id"
                       class="btn btn-warning btn-sm">
                        Редактирай
                    </a>

                    <a asp-action="Delete"
                       asp-route-id="@book.Id"
                       class="btn btn-danger btn-sm">
                        Изтрий
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
________________________________________
10. Create.cshtml
Views/Books/Create.cshtml
@model BookStore.Models.Book

@{
    ViewData["Title"] = "Добави книга";
}

<h1>Добави книга</h1>

<form asp-action="Create" method="post">

    <div class="mb-3">
        <label asp-for="Title" class="form-label">
            Заглавие
        </label>

        <input asp-for="Title" class="form-control" />

        <span asp-validation-for="Title"
              class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Author" class="form-label">
            Автор
        </label>

        <input asp-for="Author" class="form-control" />

        <span asp-validation-for="Author"
              class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Price" class="form-label">
            Цена
        </label>

        <input asp-for="Price" class="form-control" />

        <span asp-validation-for="Price"
              class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-success">
        Запази
    </button>

    <a asp-action="Index" class="btn btn-secondary">
        Отказ
    </a>

</form>
:::

---

# 11. Edit.cshtml

:::writing{variant="document" id="36174" title="Views/Books/Edit.cshtml"}
```html
@model BookStore.Models.Book

@{
    ViewData["Title"] = "Редактиране";
}

<h1>Редактиране на книга</h1>

<form asp-action="Edit" method="post">

    <input type="hidden" asp-for="Id" />

    <div class="mb-3">
        <label asp-for="Title" class="form-label">
            Заглавие
        </label>

        <input asp-for="Title" class="form-control" />

        <span asp-validation-for="Title"
              class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Author" class="form-label">
            Автор
        </label>

        <input asp-for="Author" class="form-control" />

        <span asp-validation-for="Author"
              class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Price" class="form-label">
            Цена
        </label>

        <input asp-for="Price" class="form-control" />

        <span asp-validation-for="Price"
              class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">
        Запази промените
    </button>

    <a asp-action="Index" class="btn btn-secondary">
        Назад
    </a>

</form>
:::

---

# 12. Details.cshtml

:::writing{variant="document" id="71529" title="Views/Books/Details.cshtml"}
```html
@model BookStore.Models.Book

@{
    ViewData["Title"] = "Детайли";
}

<h1>Детайли за книгата</h1>

<div class="card">
    <div class="card-body">

        <h3>@Model.Title</h3>

        <p>
            <strong>Автор:</strong>
            @Model.Author
        </p>

        <p>
            <strong>Цена:</strong>
            @Model.Price.ToString("0.00") лв.
        </p>

        <a asp-action="Edit"
           asp-route-id="@Model.Id"
           class="btn btn-warning">
            Редактирай
        </a>

        <a asp-action="Index"
           class="btn btn-secondary">
            Назад
        </a>

    </div>
</div>
:::

---

# 13. Delete.cshtml

:::writing{variant="document" id="54718" title="Views/Books/Delete.cshtml"}
```html
@model BookStore.Models.Book

@{
    ViewData["Title"] = "Изтриване";
}

<h1>Изтриване</h1>

<div class="alert alert-danger">
    Сигурни ли сте, че искате да изтриете тази книга?
</div>

<div>
    <strong>Заглавие:</strong>
    @Model.Title
</div>

<div>
    <strong>Автор:</strong>
    @Model.Author
</div>

<div>
    <strong>Цена:</strong>
    @Model.Price.ToString("0.00") лв.
</div>

<br />

<form asp-action="Delete" method="post">

    <input type="hidden" asp-for="Id" />

    <button type="submit" class="btn btn-danger">
        Да, изтрий
    </button>

    <a asp-action="Index"
       class="btn btn-secondary">
        Отказ
    </a>

</form>
:::

---

# 14. Добави меню към сайта

Отвори:

```text
Views/Shared/_Layout.cshtml
Намери менюто и добави:
<li class="nav-item">
    <a class="nav-link text-dark"
       asp-controller="Books"
       asp-action="Index">
        Книги
    </a>
</li>
Така в горното меню ще имаш:
Home | Privacy | Книги
________________________________________
15. Стартирай проекта
Натисни:
Ctrl + F5
След това отвори:
/Books
Ще видиш страницата с книгите.
Можеш да добавиш например:
Заглавие: Под игото
Автор: Иван Вазов
Цена: 15
Натискаш Запази.
Книгата се записва в SQLite базата.
________________________________________
Какво вече имаш
Това вече е реален MVC проект:
                 ASP.NET CORE MVC
                       │
          ┌────────────┼────────────┐
          ↓            ↓            ↓
        MODEL      CONTROLLER       VIEW
          │            │            │
       Book.cs    BooksController   .cshtml
          │            │            │
          └────────────┼────────────┘
                       ↓
                Entity Framework
                       ↓
                    SQLite
И имаш всички основни операции:
/Books
    ↓
Списък с книги

/Books/Create
    ↓
Добавяне

/Books/Details/1
    ↓
Детайли

/Books/Edit/1
    ↓
Редактиране

/Books/Delete/1
    ↓
Изтриване
Това е напълно достатъчен малък университетски/училищен ASP.NET Core MVC проект, върху който после могат да се добавят търсене, категории, потребители и други функции.
1.	може ли да го направиш с тях
2.	ChatGPT said:
Да. Можем да го разширим, така че проектът да има книги + категории + търсене + потребители, но пак да остане сравнително малък.
Предлагам крайната версия да бъде:
📚 BookStore – разширен MVC проект
Функции
o	Книги
	Добавяне
	Преглед
	Редактиране
	Изтриване
	Търсене по заглавие и автор
o	Категории
	Например: Роман, Фантастика, История, Учебна литература
	Всяка книга принадлежи към категория
o	Потребители
	Регистрация
	Вход
	Изход
o	Авторизация
	Само влезли потребители могат да добавят/редактират/изтриват книги
o	SQLite база данни
o	Entity Framework Core
o	Bootstrap интерфейс
Крайната структура
BookStore
│
├── Controllers
│   ├── HomeController.cs
│   ├── BooksController.cs
│   ├── CategoriesController.cs
│   └── AccountController.cs
│
├── Data
│   └── ApplicationDbContext.cs
│
├── Models
│   ├── Book.cs
│   ├── Category.cs
│   └── User.cs
│
├── Views
│   ├── Home
│   ├── Books
│   │   ├── Index.cshtml
│   │   ├── Create.cshtml
│   │   ├── Edit.cshtml
│   │   ├── Details.cshtml
│   │   └── Delete.cshtml
│   │
│   ├── Categories
│   │   ├── Index.cshtml
│   │   ├── Create.cshtml
│   │   └── Edit.cshtml
│   │
│   ├── Account
│   │   ├── Login.cshtml
│   │   └── Register.cshtml
│   │
│   └── Shared
│       └── _Layout.cshtml
│
├── wwwroot
│
├── appsettings.json
├── Program.cs
└── bookstore.db
Връзката между таблиците
User
 │
 │
 └──────────────┐
                │
Category        │
 │              │
 │              ↓
 └──────────→  Book
Например:
Категория: Фантастика

    ↓

Book
-------------------------
Id:       1
Title:    Dune
Author:   Frank Herbert
Price:    25.00
Category: Фантастика
-------------------------
Търсенето
На /Books ще имаме примерно:
Книги

[ Търси книга... ] [Търси]

-----------------------------------------
Заглавие       Автор       Категория
-----------------------------------------
Dune           Frank...    Фантастика
Под игото      И. Вазов    Роман
Тютюн          Д. Димов    Роман
-----------------------------------------
Потребители
Ще има:
Регистрация

Потребителско име: [____________]

Email:             [____________]

Парола:            [____________]

                    [Регистрация]
и:
Вход

Email:    [____________]

Парола:   [____________]

          [Вход]
След вход в менюто:
Начало | Книги | Категории | Здравей, Иван | Изход
Мога да ти го направя цялото от нулата, файл по файл, включително кода за регистрация, login, категории, търсене, CRUD и база данни, така че да можеш директно да го изградиш във Visual Studio.
1.	да
2.	ChatGPT said:
Да. Нека направим цялостен малък BookStore проект, но ще го направим по правилния ASP.NET Core начин: за потребителите ще използваме ASP.NET Core Identity, вместо да пишем собствена система за пароли.
Ще получиш:
o	📚 книги — CRUD
o	🗂️ категории
o	🔎 търсене по заглавие/автор
o	👤 регистрация и вход
o	🔐 права за добавяне/редактиране/изтриване
o	🗄️ SQLite
o	Entity Framework Core
o	Bootstrap
1. Създаване на проекта
Създай:
ASP.NET Core Web App (Model-View-Controller)
Име:
BookStore
При Authentication избери:
Individual Accounts
Ако шаблонът ти предлага SQLite като database provider, избери него.
Това е важно, защото ASP.NET Core ще създаде основата за регистрация и вход.
________________________________________
2. NuGet пакетите
Отвори Package Manager Console и инсталирай:
Install-Package Microsoft.EntityFrameworkCore.Sqlite
Install-Package Microsoft.EntityFrameworkCore.Tools
Install-Package Microsoft.VisualStudio.Web.CodeGeneration.Design
Ако проектът вече съдържа Identity и Entity Framework пакетите, не е необходимо да ги инсталираш повторно.
________________________________________
3. Model – Category
Създай:
Models/Category.cs
Models/Category.cs
using System.ComponentModel.DataAnnotations;

namespace BookStore.Models
{
    public class Category
    {
        public int Id { get; set; }

        [Required(ErrorMessage = "Въведете име на категорията.")]
        [StringLength(50)]
        public string Name { get; set; } = string.Empty;

        public ICollection<Book> Books { get; set; } = new List<Book>();
    }
}
________________________________________
4. Model – Book
Създай или замени:
Models/Book.cs
Models/Book.cs
using System.ComponentModel.DataAnnotations;

namespace BookStore.Models
{
    public class Book
    {
        public int Id { get; set; }

        [Required(ErrorMessage = "Въведете заглавие.")]
        [StringLength(100)]
        public string Title { get; set; } = string.Empty;

        [Required(ErrorMessage = "Въведете автор.")]
        [StringLength(100)]
        public string Author { get; set; } = string.Empty;

        [Range(0.01, 10000)]
        public decimal Price { get; set; }

        public int CategoryId { get; set; }

        public Category? Category { get; set; }
    }
}
Тук вече имаме връзка:
Category 1 ---- * Book
Тоест една категория може да има много книги.
________________________________________
5. Database Context
Отвори:
Data/ApplicationDbContext.cs
и го направи така:
Data/ApplicationDbContext.cs
using BookStore.Models;
using Microsoft.AspNetCore.Identity.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore;

namespace BookStore.Data
{
    public class ApplicationDbContext : IdentityDbContext
    {
        public ApplicationDbContext(
            DbContextOptions<ApplicationDbContext> options)
            : base(options)
        {
        }

        public DbSet<Book> Books { get; set; }

        public DbSet<Category> Categories { get; set; }
    }
}
Така една база данни ще съдържа както:
Books
Categories
така и таблиците на Identity за потребителите.
________________________________________
6. Connection string
В appsettings.json:
appsettings.json
{
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=bookstore.db"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*"
}
________________________________________
7. Program.cs
Тук настройваме MVC, базата данни и Identity.
Program.cs
using BookStore.Data;
using Microsoft.AspNetCore.Identity;
using Microsoft.EntityFrameworkCore;

var builder = WebApplication.CreateBuilder(args);

var connectionString =
    builder.Configuration.GetConnectionString("DefaultConnection")
    ?? throw new InvalidOperationException(
        "Connection string 'DefaultConnection' not found.");

builder.Services.AddDbContext<ApplicationDbContext>(options =>
    options.UseSqlite(connectionString));

builder.Services.AddDefaultIdentity<IdentityUser>(options =>
{
    options.SignIn.RequireConfirmedAccount = false;
})
.AddEntityFrameworkStores<ApplicationDbContext>();

builder.Services.AddControllersWithViews();

var app = builder.Build();

if (!app.Environment.IsDevelopment())
{
    app.UseExceptionHandler("/Home/Error");
    app.UseHsts();
}

app.UseHttpsRedirection();

app.UseStaticFiles();

app.UseRouting();

app.UseAuthentication();
app.UseAuthorization();

app.MapControllerRoute(
    name: "default",
    pattern: "{controller=Home}/{action=Index}/{id?}");

app.MapRazorPages();

app.Run();
Особено важни са:
app.UseAuthentication();
app.UseAuthorization();
и:
app.MapRazorPages();
Те са необходими за Identity.
________________________________________
8. Създаване на базата
В Package Manager Console:
Add-Migration InitialCreate
след това:
Update-Database
Ще се създаде:
bookstore.db
Базата вече ще съдържа таблици за:
Users
Roles
UserClaims
UserLogins
UserRoles
...
Books
Categories
________________________________________
9. BooksController
Създай:
Controllers/BooksController.cs
Controllers/BooksController.cs
using BookStore.Data;
using BookStore.Models;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.Rendering;
using Microsoft.EntityFrameworkCore;

namespace BookStore.Controllers
{
    public class BooksController : Controller
    {
        private readonly ApplicationDbContext _context;

        public BooksController(ApplicationDbContext context)
        {
            _context = context;
        }

        // GET: Books
        public async Task<IActionResult> Index(string searchString)
        {
            var books = _context.Books
                .Include(b => b.Category)
                .AsQueryable();

            if (!string.IsNullOrWhiteSpace(searchString))
            {
                books = books.Where(b =>
                    b.Title.Contains(searchString) ||
                    b.Author.Contains(searchString));
            }

            ViewData["SearchString"] = searchString;

            return View(await books.ToListAsync());
        }

        // GET: Books/Details/5
        public async Task<IActionResult> Details(int? id)
        {
            if (id == null)
                return NotFound();

            var book = await _context.Books
                .Include(b => b.Category)
                .FirstOrDefaultAsync(b => b.Id == id);

            if (book == null)
                return NotFound();

            return View(book);
        }

        // GET: Books/Create
        [Authorize]
        public async Task<IActionResult> Create()
        {
            ViewBag.Categories = new SelectList(
                await _context.Categories.ToListAsync(),
                "Id",
                "Name");

            return View();
        }

        // POST: Books/Create
        [HttpPost]
        [ValidateAntiForgeryToken]
        [Authorize]
        public async Task<IActionResult> Create(Book book)
        {
            if (ModelState.IsValid)
            {
                _context.Books.Add(book);
                await _context.SaveChangesAsync();

                return RedirectToAction(nameof(Index));
            }

            ViewBag.Categories = new SelectList(
                await _context.Categories.ToListAsync(),
                "Id",
                "Name",
                book.CategoryId);

            return View(book);
        }

        // GET: Books/Edit/5
        [Authorize]
        public async Task<IActionResult> Edit(int? id)
        {
            if (id == null)
                return NotFound();

            var book = await _context.Books.FindAsync(id);

            if (book == null)
                return NotFound();

            ViewBag.Categories = new SelectList(
                await _context.Categories.ToListAsync(),
                "Id",
                "Name",
                book.CategoryId);

            return View(book);
        }

        // POST: Books/Edit/5
        [HttpPost]
        [ValidateAntiForgeryToken]
        [Authorize]
        public async Task<IActionResult> Edit(int id, Book book)
        {
            if (id != book.Id)
                return NotFound();

            if (ModelState.IsValid)
            {
                _context.Update(book);
                await _context.SaveChangesAsync();

                return RedirectToAction(nameof(Index));
            }

            ViewBag.Categories = new SelectList(
                await _context.Categories.ToListAsync(),
                "Id",
                "Name",
                book.CategoryId);

            return View(book);
        }

        // GET: Books/Delete/5
        [Authorize]
        public async Task<IActionResult> Delete(int? id)
        {
            if (id == null)
                return NotFound();

            var book = await _context.Books
                .Include(b => b.Category)
                .FirstOrDefaultAsync(b => b.Id == id);

            if (book == null)
                return NotFound();

            return View(book);
        }

        // POST: Books/Delete/5
        [HttpPost, ActionName("Delete")]
        [ValidateAntiForgeryToken]
        [Authorize]
        public async Task<IActionResult> DeleteConfirmed(int id)
        {
            var book = await _context.Books.FindAsync(id);

            if (book != null)
            {
                _context.Books.Remove(book);
                await _context.SaveChangesAsync();
            }

            return RedirectToAction(nameof(Index));
        }
    }
}
Тук вече имаме и:
[Authorize]
Това означава, че потребителят трябва да е влязъл в системата.
________________________________________
10. Books/Index.cshtml
Създай:
Views/Books/Index.cshtml
Views/Books/Index.cshtml
@model IEnumerable<BookStore.Models.Book>

@{
    ViewData["Title"] = "Книги";
}

<h1>Книги</h1>

<form asp-action="Index" method="get" class="mb-3">
    <div class="input-group">
        <input type="text"
               name="searchString"
               value="@ViewData["SearchString"]"
               class="form-control"
               placeholder="Търси по заглавие или автор..." />

        <button type="submit" class="btn btn-primary">
            Търси
        </button>
    </div>
</form>

@if (User.Identity?.IsAuthenticated == true)
{
    <p>
        <a asp-action="Create" class="btn btn-success">
            Добави книга
        </a>
    </p>
}

<table class="table table-striped">
    <thead>
        <tr>
            <th>Заглавие</th>
            <th>Автор</th>
            <th>Категория</th>
            <th>Цена</th>
            <th>Действия</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var book in Model)
        {
            <tr>
                <td>@book.Title</td>
                <td>@book.Author</td>
                <td>@book.Category?.Name</td>
                <td>@book.Price.ToString("0.00") лв.</td>

                <td>
                    <a asp-action="Details"
                       asp-route-id="@book.Id"
                       class="btn btn-info btn-sm">
                        Детайли
                    </a>

                    @if (User.Identity?.IsAuthenticated == true)
                    {
                        <a asp-action="Edit"
                           asp-route-id="@book.Id"
                           class="btn btn-warning btn-sm">
                            Редактирай
                        </a>

                        <a asp-action="Delete"
                           asp-route-id="@book.Id"
                           class="btn btn-danger btn-sm">
                            Изтрий
                        </a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
:::

Тук вече имаме търсене:

```text
[ Dune                 ] [Търси]
и филтриране по:
o	заглавие
o	автор
________________________________________
11. Books/Create.cshtml
@model BookStore.Models.Book

@{
    ViewData["Title"] = "Добавяне на книга";
}

<h1>Добавяне на книга</h1>

<form asp-action="Create" method="post">

    <div class="mb-3">
        <label asp-for="Title" class="form-label">
            Заглавие
        </label>

        <input asp-for="Title" class="form-control" />

        <span asp-validation-for="Title"
              class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Author" class="form-label">
            Автор
        </label>

        <input asp-for="Author" class="form-control" />

        <span asp-validation-for="Author"
              class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Price" class="form-label">
            Цена
        </label>

        <input asp-for="Price" class="form-control" />

        <span asp-validation-for="Price"
              class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="CategoryId" class="form-label">
            Категория
        </label>

        <select asp-for="CategoryId"
                asp-items="ViewBag.Categories"
                class="form-select">
            <option value="">-- Изберете категория --</option>
        </select>

        <span asp-validation-for="CategoryId"
              class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-success">
        Запази
    </button>

    <a asp-action="Index"
       class="btn btn-secondary">
        Назад
    </a>

</form>
:::

---

# 12. Edit.cshtml

Можеш да използваш същата форма, като направиш:

```text
Views/Books/Edit.cshtml
@model BookStore.Models.Book

@{
    ViewData["Title"] = "Редактиране";
}

<h1>Редактиране на книга</h1>

<form asp-action="Edit" method="post">

    <input type="hidden" asp-for="Id" />

    <div class="mb-3">
        <label asp-for="Title" class="form-label">
            Заглавие
        </label>

        <input asp-for="Title" class="form-control" />

        <span asp-validation-for="Title"
              class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Author" class="form-label">
            Автор
        </label>

        <input asp-for="Author" class="form-control" />

        <span asp-validation-for="Author"
              class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Price" class="form-label">
            Цена
        </label>

        <input asp-for="Price" class="form-control" />

        <span asp-validation-for="Price"
              class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="CategoryId" class="form-label">
            Категория
        </label>

        <select asp-for="CategoryId"
                asp-items="ViewBag.Categories"
                class="form-select">
            <option value="">-- Изберете категория --</option>
        </select>

        <span asp-validation-for="CategoryId"
              class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">
        Запази
    </button>

    <a asp-action="Index"
       class="btn btn-secondary">
        Назад
    </a>

</form>
:::

---

# 13. Details.cshtml

:::writing{variant="document" id="63729" title="Views/Books/Details.cshtml"}
```html
@model BookStore.Models.Book

@{
    ViewData["Title"] = "Детайли";
}

<h1>Детайли за книгата</h1>

<div class="card">
    <div class="card-body">

        <h2>@Model.Title</h2>

        <p>
            <strong>Автор:</strong>
            @Model.Author
        </p>

        <p>
            <strong>Категория:</strong>
            @Model.Category?.Name
        </p>

        <p>
            <strong>Цена:</strong>
            @Model.Price.ToString("0.00") лв.
        </p>

        @if (User.Identity?.IsAuthenticated == true)
        {
            <a asp-action="Edit"
               asp-route-id="@Model.Id"
               class="btn btn-warning">
                Редактирай
            </a>
        }

        <a asp-action="Index"
           class="btn btn-secondary">
            Назад
        </a>

    </div>
</div>
:::

---

# 14. Delete.cshtml

:::writing{variant="document" id="75184" title="Views/Books/Delete.cshtml"}
```html
@model BookStore.Models.Book

@{
    ViewData["Title"] = "Изтриване";
}

<h1>Изтриване</h1>

<div class="alert alert-danger">
    Сигурни ли сте, че искате да изтриете тази книга?
</div>

<h3>@Model.Title</h3>

<p>
    <strong>Автор:</strong> @Model.Author
</p>

<p>
    <strong>Категория:</strong> @Model.Category?.Name
</p>

<p>
    <strong>Цена:</strong> @Model.Price.ToString("0.00") лв.
</p>

<form asp-action="Delete" method="post">

    <input type="hidden" asp-for="Id" />

    <button type="submit"
            class="btn btn-danger">
        Да, изтрий
    </button>

    <a asp-action="Index"
       class="btn btn-secondary">
        Отказ
    </a>

</form>
:::

---

# 15. CategoriesController

Сега правим управлението на категориите.

Създай:

```text
Controllers/CategoriesController.cs
using BookStore.Data;
using BookStore.Models;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Microsoft.EntityFrameworkCore;

namespace BookStore.Controllers
{
    [Authorize]
    public class CategoriesController : Controller
    {
        private readonly ApplicationDbContext _context;

        public CategoriesController(ApplicationDbContext context)
        {
            _context = context;
        }

        public async Task<IActionResult> Index()
        {
            return View(await _context.Categories.ToListAsync());
        }

        public IActionResult Create()
        {
            return View();
        }

        [HttpPost]
        [ValidateAntiForgeryToken]
        public async Task<IActionResult> Create(Category category)
        {
            if (ModelState.IsValid)
            {
                _context.Categories.Add(category);
                await _context.SaveChangesAsync();

                return RedirectToAction(nameof(Index));
            }

            return View(category);
        }

        public async Task<IActionResult> Edit(int? id)
        {
            if (id == null)
                return NotFound();

            var category = await _context.Categories.FindAsync(id);

            if (category == null)
                return NotFound();

            return View(category);
        }

        [HttpPost]
        [ValidateAntiForgeryToken]
        public async Task<IActionResult> Edit(
            int id,
            Category category)
        {
            if (id != category.Id)
                return NotFound();

            if (ModelState.IsValid)
            {
                _context.Update(category);
                await _context.SaveChangesAsync();

                return RedirectToAction(nameof(Index));
            }

            return View(category);
        }
    }
}
________________________________________
16. Categories/Index.cshtml
@model IEnumerable<BookStore.Models.Category>

@{
    ViewData["Title"] = "Категории";
}

<h1>Категории</h1>

<p>
    <a asp-action="Create"
       class="btn btn-success">
        Добави категория
    </a>
</p>

<table class="table table-striped">

    <thead>
        <tr>
            <th>Име</th>
            <th>Действия</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var category in Model)
        {
            <tr>
                <td>@category.Name</td>

                <td>
                    <a asp-action="Edit"
                       asp-route-id="@category.Id"
                       class="btn btn-warning btn-sm">
                        Редактирай
                    </a>
                </td>
            </tr>
        }
    </tbody>

</table>
:::

---

# 17. Categories/Create.cshtml

:::writing{variant="document" id="54216" title="Views/Categories/Create.cshtml"}
```html
@model BookStore.Models.Category

@{
    ViewData["Title"] = "Нова категория";
}

<h1>Нова категория</h1>

<form asp-action="Create" method="post">

    <div class="mb-3">

        <label asp-for="Name"
               class="form-label">
            Име
        </label>

        <input asp-for="Name"
               class="form-control" />

        <span asp-validation-for="Name"
              class="text-danger"></span>

    </div>

    <button type="submit"
            class="btn btn-success">
        Запази
    </button>

    <a asp-action="Index"
       class="btn btn-secondary">
        Назад
    </a>

</form>
:::

---

# 18. Categories/Edit.cshtml

:::writing{variant="document" id="69157" title="Views/Categories/Edit.cshtml"}
```html
@model BookStore.Models.Category

@{
    ViewData["Title"] = "Редактиране";
}

<h1>Редактиране на категория</h1>

<form asp-action="Edit" method="post">

    <input type="hidden"
           asp-for="Id" />

    <div class="mb-3">

        <label asp-for="Name"
               class="form-label">
            Име
        </label>

        <input asp-for="Name"
               class="form-control" />

        <span asp-validation-for="Name"
              class="text-danger"></span>

    </div>

    <button type="submit"
            class="btn btn-primary">
        Запази
    </button>

    <a asp-action="Index"
       class="btn btn-secondary">
        Назад
    </a>

</form>
:::

---

# 19. Меню

Отвори:

```text
Views/Shared/_Layout.cshtml
В менюто добави:
<li class="nav-item">
    <a class="nav-link text-dark"
       asp-controller="Books"
       asp-action="Index">
        Книги
    </a>
</li>

@if (User.Identity?.IsAuthenticated == true)
{
    <li class="nav-item">
        <a class="nav-link text-dark"
           asp-controller="Categories"
           asp-action="Index">
            Категории
        </a>
    </li>
}
Шаблонът с Individual Accounts обикновено вече има готови линкове за:
Register
Login
Logout
________________________________________
20. Начални категории
За да не въвеждаш категориите ръчно всеки път, можем да добавим начални данни.
В ApplicationDbContext.cs добави:
protected override void OnModelCreating(ModelBuilder builder)
{
    base.OnModelCreating(builder);

    builder.Entity<Category>().HasData(
        new Category
        {
            Id = 1,
            Name = "Роман"
        },
        new Category
        {
            Id = 2,
            Name = "Фантастика"
        },
        new Category
        {
            Id = 3,
            Name = "История"
        },
        new Category
        {
            Id = 4,
            Name = "Учебна литература"
        }
    );
}
Тогава:
Add-Migration SeedCategories
и:
Update-Database
________________________________________
21. Крайната структура
Вече проектът ще изглежда така:
BookStore
│
├── Controllers
│   ├── HomeController.cs
│   ├── BooksController.cs
│   └── CategoriesController.cs
│
├── Data
│   └── ApplicationDbContext.cs
│
├── Models
│   ├── Book.cs
│   └── Category.cs
│
├── Views
│   │
│   ├── Books
│   │   ├── Index.cshtml
│   │   ├── Create.cshtml
│   │   ├── Edit.cshtml
│   │   ├── Details.cshtml
│   │   └── Delete.cshtml
│   │
│   ├── Categories
│   │   ├── Index.cshtml
│   │   ├── Create.cshtml
│   │   └── Edit.cshtml
│   │
│   ├── Home
│   │
│   └── Shared
│       └── _Layout.cshtml
│
├── Areas
│   └── Identity
│
├── wwwroot
│
├── appsettings.json
├── Program.cs
└── bookstore.db
Какво ще можеш да демонстрираш
При представяне на проекта можеш да покажеш следния сценарий:
1. Отваряме BookStore
        ↓
2. Виждаме списъка с книги
        ↓
3. Търсим книга по автор
        ↓
4. Регистрираме потребител
        ↓
5. Влизаме в системата
        ↓
6. Създаваме категория
        ↓
7. Добавяме книга
        ↓
8. Редактираме книгата
        ↓
9. Отваряме Details
        ↓
10. Изтриваме книгата
Това вече е завършен малък проект, а не просто пример с един Controller и един View.
Важна забележка: при различните версии на .NET/Visual Studio шаблонът за Individual Accounts може да генерира малко различни Identity файлове. Ако при теб вече има ApplicationDbContext, Program.cs или Areas/Identity, не ги трий автоматично — можем да адаптираме кода към конкретния проект.
1.	може ли връзките на таблиците и самите таблици
2.	ChatGPT said:
Да. За този BookStore проект най-ясно е да покажеш базата като ER диаграма. В твоя случай имаме основно две таблици, които ние създаваме, плюс таблиците, които ASP.NET Core Identity създава за потребителите.
1. Основните таблици
Categories
Колона	Тип	Ключ
Id	int	PK
Name	nvarchar(50)	
Пример:
Id	Name
1	Роман
2	Фантастика
3	История
4	Учебна литература
________________________________________
Books
Колона	Тип	Ключ
Id	int	PK
Title	nvarchar(100)	
Author	nvarchar(100)	
Price	decimal	
CategoryId	int	FK
Пример:
Id	Title	Author	Price	CategoryId
1	Под игото	Иван Вазов	15.00	1
2	Тютюн	Димитър Димов	18.00	1
3	Dune	Frank Herbert	25.00	2
4	Sapiens	Yuval Noah Harari	30.00	3
________________________________________
2. Връзката между таблиците
Имаме връзка:
┌─────────────────────┐
│      Categories     │
├─────────────────────┤
│ PK Id               │
│    Name             │
└──────────┬──────────┘
           │
           │ 1
           │
           │
           │ *
┌──────────▼──────────┐
│        Books        │
├─────────────────────┤
│ PK Id               │
│    Title            │
│    Author           │
│    Price            │
│ FK CategoryId       │
└─────────────────────┘
Това е връзка:
1 : N (one-to-many)
Тоест:
Една категория може да съдържа много книги.
Например:
Фантастика
   │
   ├── Dune
   ├── Foundation
   └── The Hobbit
Но една книга има една категория.
________________________________________
3. Връзката с Entity Framework
Тази връзка се реализира от:
public int CategoryId { get; set; }

public Category? Category { get; set; }
в Book.
А в Category:
public ICollection<Book> Books { get; set; }
    = new List<Book>();
Така Entity Framework разбира:
Category 1 ─────────── * Book
________________________________________
4. Потребителите
Понеже използваме ASP.NET Core Identity, системата автоматично създава таблици за потребителите.
Основната е:
AspNetUsers
Колона	Ключ
Id	PK
UserName	
NormalizedUserName	
Email	
NormalizedEmail	
EmailConfirmed	
PasswordHash	
...	
Тук е важно да се отбележи, че паролата не се съхранява като обикновен текст. Identity съхранява хеширана стойност.
________________________________________
5. Identity таблиците
При използване на IdentityDbContext ще имаш приблизително:
AspNetUsers
AspNetRoles
AspNetUserRoles
AspNetUserClaims
AspNetUserLogins
AspNetUserTokens
AspNetRoleClaims
Връзката е приблизително:
                 ┌─────────────────┐
                 │   AspNetRoles   │
                 ├─────────────────┤
                 │ PK Id           │
                 │    Name         │
                 └────────┬────────┘
                          │
                          │
                         *
                 ┌────────▼────────┐
                 │ AspNetUserRoles │
                 ├─────────────────┤
                 │ FK UserId       │
                 │ FK RoleId       │
                 └────────┬────────┘
                          │
                          │
                         *
                 ┌────────▼────────┐
                 │   AspNetUsers   │
                 ├─────────────────┤
                 │ PK Id           │
                 │ UserName        │
                 │ Email           │
                 │ PasswordHash    │
                 │ ...             │
                 └─────────────────┘
Тук AspNetUsers и AspNetRoles имат връзка много към много чрез AspNetUserRoles.
________________________________________
6. Цялата база данни
За твоя проект схемата може да бъде представена така:
                    BOOKSTORE DATABASE
                           │
          ┌────────────────┴────────────────┐
          │                                 │
          ▼                                 ▼
┌─────────────────────┐           ┌─────────────────────┐
│      Categories     │           │        Books        │
├─────────────────────┤           ├─────────────────────┤
│ PK Id               │◄──────────│ FK CategoryId       │
│    Name             │    1 : N │ PK Id               │
└─────────────────────┘           │    Title            │
                                  │    Author           │
                                  │    Price            │
                                  └─────────────────────┘


┌─────────────────────┐           ┌─────────────────────┐
│    AspNetUsers      │           │     AspNetRoles     │
├─────────────────────┤           ├─────────────────────┤
│ PK Id               │           │ PK Id               │
│ UserName            │           │ Name                │
│ Email               │           │ ...                 │
│ PasswordHash        │           └──────────┬──────────┘
│ ...                 │                      │
└──────────┬──────────┘                      │
           │                                 │
           │ 1                               │ 1
           │                                 │
           │ *                               │ *
           └──────────► AspNetUserRoles ◄────┘
                        ├──────────────────┤
                        │ FK UserId        │
                        │ FK RoleId        │
                        └──────────────────┘
7. Ако проектът трябва да е максимално прост
За учебен малък проект можеш дори да представиш само тези две твои таблици:
┌──────────────────┐
│    Categories    │
├──────────────────┤
│ PK Id            │
│ Name             │
└────────┬─────────┘
         │
         │ 1:N
         │
┌────────▼─────────┐
│      Books       │
├──────────────────┤
│ PK Id            │
│ Title            │
│ Author           │
│ Price            │
│ FK CategoryId    │
└──────────────────┘
А Identity таблиците можеш да кажеш, че се създават автоматично от ASP.NET Core Identity за регистрация, вход и управление на потребители.
Това е и най-чистият начин да обясниш базата при представяне на проекта.
1.	къде би била промяната в кода и таблиците ако връзката беше много към много
2.	ChatGPT said:
Да. При много към много (N:N) промяната е най-вече в Model-ите, базата и DbContext. При твоя BookStore най-естественият пример е:
Една книга може да има много категории, а една категория може да съдържа много книги.
Например:
Dune
 ├── Фантастика
 └── Научна фантастика

История на България
 ├── История
 └── Учебна литература
1. Как изглежда базата при 1:N
В сегашния вариант имаме:
Categories
----------------
PK Id
Name
       1
       │
       │
       N
Books
----------------
PK Id
Title
Author
Price
FK CategoryId
Books съдържа CategoryId, защото всяка книга има една категория.
________________________________________
2. Как изглежда при N:N
При много към много не можем просто да сложим CategoryId в Books, защото една книга може да има няколко категории.
Затова създаваме трета таблица, например:
BookCategories
---------------------
FK BookId
FK CategoryId
Схемата става:
┌─────────────────┐
│     Books       │
├─────────────────┤
│ PK Id           │
│ Title           │
│ Author          │
│ Price           │
└────────┬────────┘
         │
         │ 1
         │
         │ N
┌────────▼────────┐
│  BookCategories │
├─────────────────┤
│ PK/FK BookId    │
│ PK/FK CategoryId│
└────────┬────────┘
         │
         │ N
         │
         │ 1
┌────────▼────────┐
│   Categories    │
├─────────────────┤
│ PK Id           │
│ Name            │
└─────────────────┘
Това е стандартният начин за реализиране на N:N релация в релационна база данни.
________________________________________
3. Промяна в Book.cs
В сегашния вариант имаме:
public int CategoryId { get; set; }

public Category? Category { get; set; }
Тези две свойства се махат.
Вместо тях:
public ICollection<Category> Categories { get; set; }
    = new List<Category>();
Целият Book.cs става:
Models/Book.cs – N:N
using System.ComponentModel.DataAnnotations;

namespace BookStore.Models
{
    public class Book
    {
        public int Id { get; set; }

        [Required(ErrorMessage = "Въведете заглавие.")]
        [StringLength(100)]
        public string Title { get; set; } = string.Empty;

        [Required(ErrorMessage = "Въведете автор.")]
        [StringLength(100)]
        public string Author { get; set; } = string.Empty;

        [Range(0.01, 10000)]
        public decimal Price { get; set; }

        public ICollection<Category> Categories { get; set; }
            = new List<Category>();
    }
}
________________________________________
4. Промяна в Category.cs
Преди имахме:
public ICollection<Book> Books { get; set; }
    = new List<Book>();
Това всъщност може да остане.
Целият Model:
Models/Category.cs – N:N
using System.ComponentModel.DataAnnotations;

namespace BookStore.Models
{
    public class Category
    {
        public int Id { get; set; }

        [Required(ErrorMessage = "Въведете име на категорията.")]
        [StringLength(50)]
        public string Name { get; set; } = string.Empty;

        public ICollection<Book> Books { get; set; }
            = new List<Book>();
    }
}
Сега Entity Framework вижда:
Book
  ↕
many-to-many
  ↕
Category
________________________________________
5. Какво става с ApplicationDbContext
Тук имаме два варианта.
При по-новите версии на Entity Framework Core можем да оставим EF Core автоматично да създаде междинната таблица.
Например:
public DbSet<Book> Books { get; set; }

public DbSet<Category> Categories { get; set; }
EF Core ще създаде нещо подобно на:
BookCategories
----------------
BookId
CategoryId
Но аз бих препоръчал за учебен проект да конфигурираме връзката изрично, защото така е много по-лесно да обясниш проекта.
Добавяме:
ApplicationDbContext – N:N конфигурация
protected override void OnModelCreating(ModelBuilder builder)
{
    base.OnModelCreating(builder);

    builder.Entity<Book>()
        .HasMany(b => b.Categories)
        .WithMany(c => c.Books)
        .UsingEntity(j => j.ToTable("BookCategories"));

    builder.Entity<Category>().HasData(
        new Category
        {
            Id = 1,
            Name = "Роман"
        },
        new Category
        {
            Id = 2,
            Name = "Фантастика"
        },
        new Category
        {
            Id = 3,
            Name = "История"
        },
        new Category
        {
            Id = 4,
            Name = "Учебна литература"
        }
    );
}
Това казва:
Book
  HasMany Categories
       ↓
       N
       ↓
Category
  HasMany Books
________________________________________
6. Как изглеждат таблиците
Вече имаме 3 основни таблици.
Books
Id	Title	Author	Price
1	Dune	Frank Herbert	25.00
2	Под игото	Иван Вазов	15.00
3	Sapiens	Yuval Harari	30.00
Categories
Id	Name
1	Роман
2	Фантастика
3	История
4	Учебна литература
BookCategories
BookId	CategoryId
1	2
1	4
2	1
3	3
3	4
Така:
BookId = 1
Dune
    │
    ├── CategoryId = 2 → Фантастика
    └── CategoryId = 4 → Учебна литература
А:
CategoryId = 4
Учебна литература
    │
    ├── BookId = 1 → Dune
    └── BookId = 3 → Sapiens
Точно това е много към много.
________________________________________
7. Как се променя Create
Тук има по-съществена промяна.
Преди имахме:
<select asp-for="CategoryId"
        asp-items="ViewBag.Categories">
</select>
защото избирахме една категория.
При N:N искаме да можем да изберем няколко категории.
Например:
Категории:

☑ Фантастика
☐ Роман
☐ История
☑ Учебна литература
Може да използваме select multiple.
Например:
<select asp-for="CategoryIds"
        asp-items="ViewBag.Categories"
        class="form-select"
        multiple>
</select>
Но тук вече е по-добре да използваме ViewModel, вместо директно да подаваме Book.
________________________________________
8. ViewModel за Create
Създаваме:
ViewModels/BookCreateViewModel.cs
BookCreateViewModel.cs
using System.ComponentModel.DataAnnotations;
using Microsoft.AspNetCore.Mvc.Rendering;

namespace BookStore.ViewModels
{
    public class BookCreateViewModel
    {
        [Required]
        public string Title { get; set; } = string.Empty;

        [Required]
        public string Author { get; set; } = string.Empty;

        [Range(0.01, 10000)]
        public decimal Price { get; set; }

        public List<int> CategoryIds { get; set; }
            = new List<int>();

        public IEnumerable<SelectListItem> Categories { get; set; }
            = new List<SelectListItem>();
    }
}
Така:
CategoryIds
    ↓
[1, 4]
означава:
Роман?             Не
Фантастика?        Да
История?           Не
Учебна литература? Да
________________________________________
9. Create View
Вместо един избор:
<select asp-for="CategoryId">
ще имаме:
<select asp-for="CategoryIds"
        asp-items="Model.Categories"
        class="form-select"
        multiple>
</select>
Потребителят може да избере повече от една категория.
________________________________________
10. Controller
В Controller-а при създаването вече трябва да добавим избраните категории:
var categories = await _context.Categories
    .Where(c => model.CategoryIds.Contains(c.Id))
    .ToListAsync();

var book = new Book
{
    Title = model.Title,
    Author = model.Author,
    Price = model.Price,
    Categories = categories
};

_context.Books.Add(book);

await _context.SaveChangesAsync();
Entity Framework автоматично ще запише:
Books
и съответните записи в:
BookCategories
________________________________________
11. Сравнение
1:N
Category
    │
    │ 1
    │
    │ N
    ▼
 Book
Таблици:
Categories
     │
     │
     ▼
Books
Books има:
CategoryId
________________________________________
N:N
Category
    │
    │ 1       N
    └── BookCategories ──┐
                         │
                    N    │    1
                         ▼
                        Book
Таблици:
Categories
     │
     │
     ▼
BookCategories
     ▲
     │
     │
   Books
BookCategories е междинната таблица.
________________________________________
Най-важната промяна
При 1:N:
public int CategoryId { get; set; }
public Category Category { get; set; }
При N:N:
public ICollection<Category> Categories { get; set; }
    = new List<Category>();
и:
public ICollection<Book> Books { get; set; }
    = new List<Book>();
а в базата се появява:
BookCategories
----------------
BookId
CategoryId
Това е основната разлика, която е добре да можеш да обясниш при представяне на проекта.

